import { styled } from "styled-components";

export const Wrapper = styled.div`
  .ant-checkbox-wrapper {
    width: 100%;
    font-size: 18px;
  }

  .ant-checkbox-inner {
    transform: scale(1.3);
  }
`;
